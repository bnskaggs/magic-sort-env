# Also satisfies `prime env push` detection, which requires a root-level .py
# file or an immediate subdirectory containing __init__.py; the actual env
# module ships from src/magic_sort_env via the wheel.
